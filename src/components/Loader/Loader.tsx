import { FC } from "react";
import { LoaderContainer, LoaderStyle } from "@/components/style";

const Loader: FC = () => {
  return (
    <LoaderContainer>
      <LoaderStyle />
    </LoaderContainer>
  );
};
export default Loader;